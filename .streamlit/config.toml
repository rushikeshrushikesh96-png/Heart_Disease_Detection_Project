# Forces a light theme so every heading and coloured box is always readable,
# no matter what theme the viewer's browser or Streamlit is set to.
[theme]
base = "light"
primaryColor = "#6366F1"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F5F6FA"
textColor = "#1E293B"

[server]
headless = true
